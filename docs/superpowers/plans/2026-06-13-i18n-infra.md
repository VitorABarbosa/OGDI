# i18n Infraestrutura (Plano 1) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar o site OGDI trilíngue-ready (PT default sem prefixo, `/en`, `/es`) com next-intl, reestruturando o roteamento para `app/[locale]/` e ligando o seletor de idioma — com o "chrome" global (nav, footer, CTAs) já traduzido nos 3 idiomas como esqueleto que prova o pipeline ponta a ponta.

**Architecture:** next-intl v4 com segmento `[locale]`, `localePrefix: 'as-needed'`, middleware de negociação, `setRequestLocale` em todas as páginas para manter SSG. O layout raiz passa a ser `app/[locale]/layout.tsx`. Mensagens em `messages/{pt,en,es}.json` namespaced; nesta fase só os namespaces `common`/`nav`/`footer` são populados de verdade nos 3 idiomas — o corpo das páginas continua em PT (extração na Fase/Plano 2).

**Tech Stack:** Next.js 15.5.19 (App Router), TypeScript, next-intl v4, Tailwind v4, Vitest + Testing Library.

**Escopo deste plano (Plano 1):** infra + roteamento + provider + switcher + i18n do chrome global. **Fora de escopo (Plano 2):** extrair e traduzir as ~660 strings de prosa das seções de página; localizar `metadata` por página; conteúdo provisório dos `[slug]`.

---

## Estrutura de arquivos

**Criar:**
- `i18n/routing.ts` — config de locales/defaultLocale/localePrefix.
- `i18n/request.ts` — `getRequestConfig`, carrega `messages/<locale>.json`.
- `i18n/navigation.ts` — wrappers `Link`/`redirect`/`usePathname`/`useRouter`/`getPathname`.
- `middleware.ts` — middleware do next-intl (raiz do projeto).
- `messages/pt.json`, `messages/en.json`, `messages/es.json` — catálogos (namespaces `common`, `nav`, `footer`).
- `app/[locale]/layout.tsx` — layout raiz (renderiza `<html>`, provider, Header/Footer).
- `app/[locale]/not-found.tsx` — boundary de 404 para locale inválido.
- `test/intl.tsx` — helper `renderWithIntl` para testes.
- `components/layout/Header/LangSwitcher.tsx` — extrai o switcher do Header e o liga à navegação.
- `components/layout/Header/LangSwitcher.test.tsx`, `components/layout/Header/Nav.test.tsx`.

**Modificar:**
- `next.config.ts` — envolver com `createNextIntlPlugin`.
- `data/nav.ts` — `label` → `key` (texto vai para `messages.nav`).
- `components/layout/Header/Header.tsx` — usar `<LangSwitcher>` extraído; nada mais.
- `components/layout/Header/Nav.tsx` — labels via `useTranslations('nav')`.
- `components/layout/Header/MobileMenu.tsx` — labels via `useTranslations('nav')`, CTA via `common`.
- `components/layout/Footer/Footer.tsx` — labels/textos via `useTranslations`.
- `components/layout/Header/Header.test.tsx` — embrulhar com `renderWithIntl`.
- `package.json` / `package-lock.json` — dep next-intl (lockfile via Docker Linux).

**Mover (git mv) para `app/[locale]/`:**
- `app/page.tsx`, `app/layout.tsx`, e as pastas de rota: `atuacao/`, `clientes/`, `contato/`, `insights/`, `institucional/`, `investidores/`, `parceiros/`, `politica-de-privacidade/`, `projetos/`.

**Permanecem em `app/` (não-localizados):** `_sections/`, `globals.css`, `favicon.ico`, `icon.png`, `apple-icon.png`, `robots.ts`, `sitemap.ts`.

---

## Task 1: Instalar next-intl e regenerar lockfile no Linux

**Files:**
- Modify: `package.json`, `package-lock.json`

- [ ] **Step 1: Instalar a dependência**

Run (Windows dev — instala em node_modules; o lockfile resultante é descartado/regenerado no Step 2):
```bash
npm install next-intl
```
Expected: `next-intl` aparece em `dependencies` no `package.json` (v4.x).

- [ ] **Step 2: Regenerar o lockfile dentro do Docker Linux**

> Crítico: o lockfile gerado no Windows quebra o `npm ci` do Railway (árvore Tailwind oxide/emnapi). Regenerar SEMPRE em container Linux. Docker Desktop precisa estar rodando (`Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"`, aguardar ~1min). Em Git Bash, prefixar com `MSYS_NO_PATHCONV=1`.

Run (PowerShell, a partir da raiz do projeto):
```powershell
docker run --rm -v "${PWD}:/host" node:22 bash -lc "mkdir -p /build; cp /host/package*.json /build/; cd /build; npm install; rm -rf node_modules; npm ci; cp package-lock.json /host/"
```
Expected: o `npm ci` dentro do container completa sem "Missing ... from lock file"; `package-lock.json` atualizado no host.

- [ ] **Step 3: Verificar o build local ainda funciona**

Run:
```bash
npx tsc --noEmit
```
Expected: sem erros (next-intl traz seus próprios tipos).

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json
git commit -m "build(i18n): adiciona next-intl e regenera lockfile no Linux"
```

---

## Task 2: Config de roteamento, request e navigation do next-intl

**Files:**
- Create: `i18n/routing.ts`, `i18n/request.ts`, `i18n/navigation.ts`

- [ ] **Step 1: Criar `i18n/routing.ts`**

```ts
import { defineRouting } from "next-intl/routing";

export const routing = defineRouting({
  locales: ["pt", "en", "es"],
  defaultLocale: "pt",
  // PT na raiz sem prefixo; EN/ES com prefixo (/en, /es)
  localePrefix: "as-needed",
});

export type Locale = (typeof routing.locales)[number];
```

- [ ] **Step 2: Criar `i18n/request.ts`**

```ts
import { getRequestConfig } from "next-intl/server";
import { hasLocale } from "next-intl";
import { routing } from "./routing";

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested)
    ? requested
    : routing.defaultLocale;

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 3: Criar `i18n/navigation.ts`**

```ts
import { createNavigation } from "next-intl/navigation";
import { routing } from "./routing";

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

- [ ] **Step 4: Verificar tipos**

Run:
```bash
npx tsc --noEmit
```
Expected: sem erros.

- [ ] **Step 5: Commit**

```bash
git add i18n/
git commit -m "feat(i18n): config routing/request/navigation do next-intl"
```

---

## Task 3: Plugin no next.config e middleware

**Files:**
- Modify: `next.config.ts`
- Create: `middleware.ts`

- [ ] **Step 1: Envolver `next.config.ts` com o plugin**

```ts
import type { NextConfig } from "next";
import createNextIntlPlugin from "next-intl/plugin";

const withNextIntl = createNextIntlPlugin("./i18n/request.ts");

const nextConfig: NextConfig = {
  /* config options here */
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 2: Criar `middleware.ts` na raiz**

```ts
import createMiddleware from "next-intl/middleware";
import { routing } from "./i18n/routing";

export default createMiddleware(routing);

export const config = {
  // Roda em todas as rotas exceto APIs, internos do Next, e arquivos com extensão
  // (assets, favicon, robots, sitemap, etc.)
  matcher: ["/((?!api|_next|_vercel|.*\\..*).*)"],
};
```

- [ ] **Step 3: Verificar tipos**

Run:
```bash
npx tsc --noEmit
```
Expected: sem erros.

- [ ] **Step 4: Commit**

```bash
git add next.config.ts middleware.ts
git commit -m "feat(i18n): plugin next-intl no config + middleware de locale"
```

---

## Task 4: Catálogos de mensagem do chrome global (PT/EN/ES)

**Files:**
- Create: `messages/pt.json`, `messages/en.json`, `messages/es.json`

> Apenas namespaces globais nesta fase. `nav` cobre os 6 itens; `common` cobre CTAs/labels compartilhados; `footer` cobre textos do rodapé. Os demais namespaces de página entram no Plano 2.

- [ ] **Step 1: Criar `messages/pt.json`**

```json
{
  "nav": {
    "institucional": "Institucional",
    "projetos": "Projetos",
    "clientes": "Clientes",
    "investidores": "Investidores",
    "insights": "Insights",
    "contato": "Contato"
  },
  "common": {
    "ctaHeader": "Fale com a Open Group",
    "ctaMobile": "Apresentar oportunidade",
    "langLabel": "Idioma: Português (Brasil). Ver outros idiomas"
  },
  "footer": {
    "tagline": "O valor nasce antes da obra.",
    "atuacaoTitle": "Atuação",
    "navTitle": "Navegação",
    "privacy": "Política de Privacidade"
  }
}
```

- [ ] **Step 2: Criar `messages/en.json`**

```json
{
  "nav": {
    "institucional": "About",
    "projetos": "Projects",
    "clientes": "Clients",
    "investidores": "Investors",
    "insights": "Insights",
    "contato": "Contact"
  },
  "common": {
    "ctaHeader": "Talk to Open Group",
    "ctaMobile": "Present an opportunity",
    "langLabel": "Language: English. View other languages"
  },
  "footer": {
    "tagline": "Value is created before ground is broken.",
    "atuacaoTitle": "What we do",
    "navTitle": "Navigation",
    "privacy": "Privacy Policy"
  }
}
```

- [ ] **Step 3: Criar `messages/es.json`**

```json
{
  "nav": {
    "institucional": "Institucional",
    "projetos": "Proyectos",
    "clientes": "Clientes",
    "investidores": "Inversores",
    "insights": "Insights",
    "contato": "Contacto"
  },
  "common": {
    "ctaHeader": "Habla con Open Group",
    "ctaMobile": "Presentar una oportunidad",
    "langLabel": "Idioma: Español. Ver otros idiomas"
  },
  "footer": {
    "tagline": "El valor nace antes de la obra.",
    "atuacaoTitle": "Qué hacemos",
    "navTitle": "Navegación",
    "privacy": "Política de Privacidad"
  }
}
```

- [ ] **Step 4: Commit**

```bash
git add messages/
git commit -m "feat(i18n): catalogos do chrome global (nav/common/footer) PT/EN/ES"
```

---

## Task 5: Mover rotas para `app/[locale]/` e adaptar imports

**Files:**
- Move: `app/page.tsx`, `app/layout.tsx`, pastas de rota → `app/[locale]/`
- Modify: `app/[locale]/page.tsx` (imports relativos → alias)

- [ ] **Step 1: Criar a pasta e mover as rotas**

Run (PowerShell):
```powershell
New-Item -ItemType Directory -Force "app/[locale]" | Out-Null
git mv app/page.tsx "app/[locale]/page.tsx"
git mv app/layout.tsx "app/[locale]/layout.tsx"
foreach ($d in "atuacao","clientes","contato","insights","institucional","investidores","parceiros","politica-de-privacidade","projetos") { git mv "app/$d" "app/[locale]/$d" }
```
Expected: `git status` mostra os renames; `app/_sections`, `globals.css`, `robots.ts`, `sitemap.ts`, ícones permanecem em `app/`.

- [ ] **Step 2: Corrigir imports relativos do `app/[locale]/page.tsx`**

Os imports `./_sections/...` agora apontam para `app/[locale]/_sections` (inexistente). Trocar para o alias `@/app/_sections/...`. Conteúdo final do topo do arquivo:

```tsx
import { Hero } from "@/app/_sections/Hero/Hero";
import { Projetos } from "@/app/_sections/Projetos/Projetos";
import { Atuacao } from "@/app/_sections/Atuacao/Atuacao";
import { Modelos } from "@/app/_sections/Modelos/Modelos";
import { Manifesto } from "@/app/_sections/Manifesto/Manifesto";
import { Clientes } from "@/app/_sections/Clientes/Clientes";
import { Contato } from "@/app/_sections/Contato/Contato";
import { PageSectionRail } from "@/app/_sections/PageSectionRail";
import { IntroVideo } from "@/components/Intro/IntroVideo";
import { RevealController } from "@/components/Reveal/RevealController";
```
(O restante do arquivo permanece igual.)

- [ ] **Step 3: Verificar se restaram imports relativos quebrados**

Run:
```bash
grep -rn "from \"\./_sections\|from \"\.\./_sections\|from \"\./globals" "app/[locale]" || echo "OK sem relativos quebrados"
```
Expected: `OK sem relativos quebrados` (ou nenhuma linha problemática). As páginas em pastas (ex. `institucional/page.tsx`) já usam `@/app/_sections/...` e não precisam de mudança.

- [ ] **Step 4: Verificar tipos**

Run:
```bash
npx tsc --noEmit
```
Expected: sem erros (o layout ainda será reescrito na Task 6; tsc pode passar mas o build virá depois).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "refactor(i18n): move rotas para app/[locale] e ajusta imports"
```

---

## Task 6: Layout raiz do locale com provider e SSG

**Files:**
- Modify: `app/[locale]/layout.tsx`

- [ ] **Step 1: Reescrever `app/[locale]/layout.tsx`**

```tsx
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { hasLocale, NextIntlClientProvider } from "next-intl";
import { setRequestLocale, getTranslations } from "next-intl/server";
import { serif, sans, news } from "@/lib/fonts";
import { Header } from "@/components/layout/Header/Header";
import { Footer } from "@/components/layout/Footer/Footer";
import { LenisProvider } from "@/components/Lenis/LenisProvider";
import { routing } from "@/i18n/routing";
import "@/app/globals.css";

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

const ogLocale: Record<string, string> = { pt: "pt_BR", en: "en_US", es: "es_ES" };

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>;
}): Promise<Metadata> {
  const { locale } = await params;
  return {
    metadataBase: new URL("https://opengroup.com.br"),
    title: {
      default: "Open Group — Desenvolvimento Imobiliário",
      template: "%s · Open Group",
    },
    description:
      "Estruturação e desenvolvimento de empreendimentos imobiliários. O valor nasce antes da obra.",
    openGraph: {
      type: "website",
      locale: ogLocale[locale] ?? "pt_BR",
      siteName: "Open Group",
      title: "Open Group — Desenvolvimento Imobiliário",
      description: "Estruturação e desenvolvimento de empreendimentos imobiliários.",
      images: [{ url: "/assets/logos/og-logo.png", width: 1200, height: 630, alt: "Open Group" }],
    },
    twitter: { card: "summary_large_image" },
  };
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  if (!hasLocale(routing.locales, locale)) notFound();
  setRequestLocale(locale);

  return (
    <html lang={locale} className={`${serif.variable} ${sans.variable} ${news.variable}`}>
      <body>
        <NextIntlClientProvider>
          <LenisProvider>
            <Header />
            {children}
            <Footer />
          </LenisProvider>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

> Nota: `NextIntlClientProvider` sem props herda locale e mensagens do request (next-intl v4). Isso disponibiliza todos os namespaces aos client components — aceitável para o tamanho do site.

- [ ] **Step 2: Criar `app/[locale]/not-found.tsx`**

```tsx
export default function NotFound() {
  return (
    <main className="min-h-[60vh] grid place-items-center px-pad-x">
      <p className="font-serif text-[clamp(28px,4vw,48px)] text-ink">404</p>
    </main>
  );
}
```

- [ ] **Step 3: Adicionar `setRequestLocale` em cada página de rota**

Para manter SSG, cada `page.tsx` sob `app/[locale]/` que recebe `params` deve chamar `setRequestLocale(locale)` no topo. Páginas estáticas sem `params` (ex. `institucional/page.tsx`) precisam passar a receber `params: Promise<{ locale: string }>` e chamá-lo. Exemplo para `app/[locale]/institucional/page.tsx` (adicionar import e tornar a função async):

```tsx
import { setRequestLocale } from "next-intl/server";
// ...imports existentes...

export default async function InstitucionalPage({
  params,
}: {
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  setRequestLocale(locale);
  return (
    // ...JSX existente inalterado...
  );
}
```

Aplicar o mesmo padrão (receber `params`, `await`, `setRequestLocale`) a: `page.tsx` (home), `atuacao`, `clientes`, `contato`, `insights`, `investidores`, `parceiros`, `politica-de-privacidade`, `projetos/page.tsx` e `projetos/[slug]/page.tsx`. No `[slug]`, o `params` passa a ser `Promise<{ locale: string; slug: string }>` — atualizar o tipo no `Page` e no `generateMetadata`, e chamar `setRequestLocale(locale)`.

- [ ] **Step 4: Build completo**

Run:
```bash
npm run build
```
Expected: build conclui; saída lista as rotas sob `/[locale]` geradas estaticamente para `pt`, `en`, `es` (incluindo os 6 slugs × 3 = 18 páginas de empreendimento). Sem aviso de renderização dinâmica forçada.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(i18n): layout do locale com provider, metadata e SSG via setRequestLocale"
```

---

## Task 7: Helper de teste `renderWithIntl`

**Files:**
- Create: `test/intl.tsx`

- [ ] **Step 1: Criar o helper**

```tsx
import { render } from "@testing-library/react";
import { NextIntlClientProvider } from "next-intl";
import ptMessages from "@/messages/pt.json";

// Renderiza componentes que usam next-intl com as mensagens PT reais.
export function renderWithIntl(
  ui: React.ReactElement,
  { locale = "pt", messages = ptMessages }: { locale?: string; messages?: Record<string, unknown> } = {},
) {
  return render(
    <NextIntlClientProvider locale={locale} messages={messages}>
      {ui}
    </NextIntlClientProvider>,
  );
}
```

- [ ] **Step 2: Verificar tipos**

Run:
```bash
npx tsc --noEmit
```
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add test/intl.tsx
git commit -m "test(i18n): helper renderWithIntl com mensagens PT"
```

---

## Task 8: Nav e MobileMenu via traduções

**Files:**
- Modify: `data/nav.ts`, `components/layout/Header/Nav.tsx`, `components/layout/Header/MobileMenu.tsx`
- Create: `components/layout/Header/Nav.test.tsx`

- [ ] **Step 1: Escrever o teste do Nav (falha primeiro)**

`components/layout/Header/Nav.test.tsx`:
```tsx
import { screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import { renderWithIntl } from "@/test/intl";
import { Nav } from "./Nav";

describe("Nav", () => {
  it("renderiza os rotulos a partir das traducoes", () => {
    renderWithIntl(<Nav onDark={false} />);
    expect(screen.getByRole("link", { name: "Institucional" })).toBeInTheDocument();
    expect(screen.getByRole("link", { name: "Projetos" })).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run:
```bash
npx vitest run components/layout/Header/Nav.test.tsx
```
Expected: FALHA (Nav ainda usa `it.label` direto; sem provider o `useTranslations` lança, ou os labels não batem).

- [ ] **Step 3: Reestruturar `data/nav.ts` (label → key)**

```ts
export type NavItem = { key: string; href: string };

export const navItems: NavItem[] = [
  { key: "institucional", href: "/institucional" },
  { key: "projetos", href: "/projetos" },
  { key: "clientes", href: "/clientes" },
  { key: "investidores", href: "/investidores" },
  { key: "insights", href: "/insights" },
  { key: "contato", href: "/contato" },
];

// footerAtuacao permanece como está por enquanto (traduzido no Plano 2).
export const footerAtuacao: { label: string; href: string }[] = [
  { label: "Leitura da oportunidade", href: "/#atuacao" },
  { label: "Inteligência de mercado", href: "/#atuacao" },
  { label: "Viabilidade", href: "/#atuacao" },
  { label: "Estruturação da operação", href: "/#atuacao" },
  { label: "Conexão com parceiros", href: "/#atuacao" },
  { label: "Preparação para lançamento", href: "/#atuacao" },
];
```

- [ ] **Step 4: Atualizar `Nav.tsx`**

```tsx
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
import { navItems } from "@/data/nav";
import { cn } from "@/lib/cn";

const headerItems = navItems.filter((it) => it.key !== "contato");

export function Nav({ onDark }: { onDark: boolean }) {
  const t = useTranslations("nav");
  return (
    <nav aria-label="Principal" className="hidden lg:flex items-center gap-[clamp(20px,1.8vw,34px)]">
      {headerItems.map((it) => (
        <Link key={it.key} href={it.href}
          className={cn("group relative text-[12px] tracking-[.1em] py-1 transition-colors duration-200",
            onDark ? "text-white hover:text-white" : "text-ink-2 hover:text-ink")}>
          {t(it.key)}
          <span className="absolute left-0 bottom-0 h-px w-0 bg-current transition-[width] duration-300 ease-brand group-hover:w-full" />
        </Link>
      ))}
    </nav>
  );
}
```

> `Link` agora vem de `@/i18n/navigation` (mantém o locale ativo na navegação).

- [ ] **Step 5: Atualizar `MobileMenu.tsx`**

```tsx
"use client";
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
import { navItems } from "@/data/nav";
import { site } from "@/data/site";
import { Button } from "@/components/ui/Button";
import { cn } from "@/lib/cn";

export function MobileMenu({ open, onClose }: { open: boolean; onClose: () => void }) {
  const t = useTranslations("nav");
  const tc = useTranslations("common");
  return (
    <div aria-hidden={!open} className={cn(
      "fixed inset-0 z-[90] bg-dark text-on-dark px-pad-x pt-[120px] pb-12 flex flex-col justify-between",
      "transition-transform duration-[600ms] ease-brand",
      open ? "translate-y-0 visible" : "-translate-y-full invisible")}>
      <nav className="flex flex-col">
        {navItems.map((it) => (
          <Link key={it.key} href={it.href} onClick={onClose}
            className="font-sans font-medium text-[26px] tracking-[-.02em] py-[14px] border-b border-[color:var(--line-dark)] text-on-dark hover:text-green">
            {t(it.key)}
          </Link>
        ))}
      </nav>
      <div className="flex flex-col gap-[6px] text-on-dark-2 text-[14px]">
        <Button href="/contato" variant="light" className="self-start mb-[18px]">{tc("ctaMobile")}</Button>
        <span>{site.email}</span>
        <span>{site.location}</span>
      </div>
    </div>
  );
}
```

- [ ] **Step 6: Rodar o teste do Nav e ver passar**

Run:
```bash
npx vitest run components/layout/Header/Nav.test.tsx
```
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add data/nav.ts components/layout/Header/Nav.tsx components/layout/Header/MobileMenu.tsx components/layout/Header/Nav.test.tsx
git commit -m "feat(i18n): Nav e MobileMenu via traducoes do namespace nav"
```

---

## Task 9: Extrair o LangSwitcher e ligá-lo à navegação

**Files:**
- Create: `components/layout/Header/LangSwitcher.tsx`, `components/layout/Header/LangSwitcher.test.tsx`
- Modify: `components/layout/Header/Header.tsx`

- [ ] **Step 1: Escrever o teste do LangSwitcher (falha primeiro)**

`components/layout/Header/LangSwitcher.test.tsx`:
```tsx
import { screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import { renderWithIntl } from "@/test/intl";
import { LangSwitcher } from "./LangSwitcher";

const replace = vi.fn();
vi.mock("@/i18n/navigation", () => ({
  usePathname: () => "/institucional",
  useRouter: () => ({ replace }),
}));

describe("LangSwitcher", () => {
  it("oferece os outros idiomas como botoes", () => {
    renderWithIntl(<LangSwitcher light={false} locale="pt" />);
    expect(screen.getByRole("button", { name: "EN" })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: "ES" })).toBeInTheDocument();
  });

  it("navega para o mesmo caminho no locale escolhido", () => {
    renderWithIntl(<LangSwitcher light={false} locale="pt" />);
    screen.getByRole("button", { name: "EN" }).click();
    expect(replace).toHaveBeenCalledWith("/institucional", { locale: "en" });
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run:
```bash
npx vitest run components/layout/Header/LangSwitcher.test.tsx
```
Expected: FALHA (módulo `./LangSwitcher` não existe).

- [ ] **Step 3: Criar `components/layout/Header/LangSwitcher.tsx`**

```tsx
"use client";
import { useTranslations } from "next-intl";
import { usePathname, useRouter } from "@/i18n/navigation";
import { cn } from "@/lib/cn";

const ALL = [
  { code: "pt", label: "PT" },
  { code: "en", label: "EN" },
  { code: "es", label: "ES" },
] as const;

// Seletor de idioma: o locale ativo fica visível; os outros deslizam inline no
// hover. Clicar troca o locale preservando a rota atual (via next-intl router).
export function LangSwitcher({ light, locale }: { light: boolean; locale: string }) {
  const t = useTranslations("common");
  const pathname = usePathname();
  const router = useRouter();
  const type = "font-sans text-[11px] tracking-[.18em] uppercase font-medium";
  const active = ALL.find((l) => l.code === locale) ?? ALL[0];
  const others = ALL.filter((l) => l.code !== active.code);

  return (
    <div className={cn("group/lang flex items-center py-[6px]", light ? "text-white" : "text-ink")}>
      <div className="grid grid-cols-[0fr] transition-[grid-template-columns] duration-[420ms] ease-brand group-hover/lang:grid-cols-[1fr] group-focus-within/lang:grid-cols-[1fr]">
        <div className="flex min-w-0 items-center gap-[14px] overflow-hidden opacity-0 transition-opacity duration-300 ease-brand group-hover/lang:opacity-100 group-focus-within/lang:opacity-100">
          {others.map((l) => (
            <button key={l.code} type="button" onClick={() => router.replace(pathname, { locale: l.code })}
              className={cn(type, "cursor-pointer opacity-50 transition-opacity duration-200 hover:opacity-100 focus-visible:opacity-100")}>
              {l.label}
            </button>
          ))}
          <span aria-hidden className="mr-[14px] h-[11px] w-px shrink-0 bg-current opacity-30" />
        </div>
      </div>
      <button type="button" aria-haspopup="true" aria-label={t("langLabel")}
        className={cn(type, "flex cursor-default items-center gap-[9px]")}>
        {active.label}
      </button>
    </div>
  );
}
```

- [ ] **Step 4: Atualizar `Header.tsx` para usar o switcher extraído**

Edições precisas (o arquivo é o lido em `components/layout/Header/Header.tsx`):

1. **Imports.** Trocar `import Link from "next/link";` por `import { Link } from "@/i18n/navigation";` (isso localiza de uma vez os dois `Link` usados — logo e CTA). Adicionar abaixo dos imports existentes:
```tsx
import { useLocale, useTranslations } from "next-intl";
import { LangSwitcher } from "./LangSwitcher";
```

2. **Remover** do fim do arquivo a função local `function LangSwitcher(...)` inteira e a const `const otherLangs = [...]` (passam para `LangSwitcher.tsx`).

3. **Dentro de `Header()`**, logo após `const menu = useMobileMenu();`, adicionar:
```tsx
const locale = useLocale();
const tc = useTranslations("common");
```

4. **Trocar** `<LangSwitcher light={lightForeground} />` por:
```tsx
<LangSwitcher light={lightForeground} locale={locale} />
```

5. **Trocar** o texto fixo do CTA `Fale com a Open Group` (dentro do `<Link href="/contato" ...>`) por `{tc("ctaHeader")}`.

> O `<Link href="/#top">` do logo e o `<Link href="/contato">` do CTA agora usam o `Link` localizado importado no item 1 — sem outra mudança.

- [ ] **Step 5: Rodar o teste do LangSwitcher e ver passar**

Run:
```bash
npx vitest run components/layout/Header/LangSwitcher.test.tsx
```
Expected: PASS (2 testes).

- [ ] **Step 6: Commit**

```bash
git add components/layout/Header/LangSwitcher.tsx components/layout/Header/LangSwitcher.test.tsx components/layout/Header/Header.tsx
git commit -m "feat(i18n): liga o seletor de idioma a navegacao next-intl"
```

---

## Task 10: Footer via traduções e Header.test corrigido

**Files:**
- Modify: `components/layout/Footer/Footer.tsx`, `components/layout/Header/Header.test.tsx`

> O Footer é Server Component → usar `getTranslations` (async). A coluna "Navegação" passa a renderizar `navItems` (agora `key`) com labels traduzidos; a coluna "Atuação" mantém `footerAtuacao` (shape `label`, PT, deferido). O `FooterCol` genérico continua servindo só a coluna de Atuação.

- [ ] **Step 1: Reescrever `components/layout/Footer/Footer.tsx`**

```tsx
import Image from "next/image";
import { getTranslations } from "next-intl/server";
import { Link } from "@/i18n/navigation";
import { navItems, footerAtuacao } from "@/data/nav";
import { site } from "@/data/site";
import { Icon } from "@/components/ui/Icon";

export async function Footer() {
  const tn = await getTranslations("nav");
  const tf = await getTranslations("footer");
  return (
    <footer className="bg-dark-2 text-on-dark px-pad-x pt-[clamp(64px,7vw,104px)] pb-9">
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-[1.7fr_1fr_1fr_1.1fr] gap-12 pb-16 border-b border-[color:var(--line-dark)]">
        <div>
          <Link href="/#top" className="flex items-center gap-[11px]">
            <Image src="/assets/logos/og-logo-light.png" alt="Open Group" width={34} height={34} className="w-[34px] h-[34px] object-contain" />
            <span className="font-sans font-medium text-[14.5px] leading-none text-white">
              {site.name}<small className="block text-[8.5px] tracking-[.24em] uppercase mt-1 text-on-dark-2 font-normal">{site.subtitle}</small>
            </span>
          </Link>
          <p className="font-serif italic font-light text-[22px] text-on-dark mt-6 max-w-[280px] leading-[1.35]">{tf("tagline")}</p>
        </div>
        <div>
          <h5 className="text-[11px] tracking-[.16em] uppercase text-on-dark-2 mb-5 font-medium">{tf("navTitle")}</h5>
          <ul className="list-none p-0 m-0 flex flex-col gap-3">
            {navItems.map((it) => (
              <li key={it.key}><Link href={it.href} className="text-[14px] text-on-dark/80 hover:text-green transition-colors">{tn(it.key)}</Link></li>
            ))}
          </ul>
        </div>
        <FooterCol title={tf("atuacaoTitle")} items={footerAtuacao} />
        <div>
          <h5 className="text-[11px] tracking-[.16em] uppercase text-on-dark-2 mb-5 font-medium">{tn("contato")}</h5>
          <ul className="list-none p-0 m-0 flex flex-col gap-3 text-[14px]">
            <li><a href={`mailto:${site.email}`} className="text-on-dark/80 hover:text-green transition-colors">{site.email}</a></li>
            <li><Link href="/contato" className="text-on-dark/80 hover:text-green transition-colors">{site.phone}</Link></li>
            <li className="text-on-dark-2">{site.location}</li>
          </ul>
        </div>
      </div>
      <div className="mt-8 flex items-center justify-between flex-wrap gap-4">
        <div className="text-[12.5px] text-on-dark-2">{site.copyright} · <Link href="/politica-de-privacidade" className="text-on-dark/70 transition-colors hover:text-green">{tf("privacy")}</Link></div>
        <div className="flex gap-2">
          <a href={site.social.linkedin} aria-label="LinkedIn" className="w-10 h-10 border border-[color:var(--line-dark)] inline-flex items-center justify-center text-on-dark hover:bg-white hover:text-dark hover:border-white transition-colors"><Icon name="linkedin" /></a>
          <a href={site.social.instagram} aria-label="Instagram" className="w-10 h-10 border border-[color:var(--line-dark)] inline-flex items-center justify-center text-on-dark hover:bg-white hover:text-dark hover:border-white transition-colors"><Icon name="instagram" /></a>
        </div>
      </div>
    </footer>
  );
}

function FooterCol({ title, items }: { title: string; items: { label: string; href: string }[] }) {
  return (
    <div>
      <h5 className="text-[11px] tracking-[.16em] uppercase text-on-dark-2 mb-5 font-medium">{title}</h5>
      <ul className="list-none p-0 m-0 flex flex-col gap-3">
        {items.map((it) => (
          <li key={it.label}><Link href={it.href} className="text-[14px] text-on-dark/80 hover:text-green transition-colors">{it.label}</Link></li>
        ))}
      </ul>
    </div>
  );
}
```

> `site.copyright` e `footerAtuacao` permanecem PT nesta fase (data em `site.ts`/`nav.ts`) — traduzidos no Plano 2.

- [ ] **Step 2: Corrigir `components/layout/Header/Header.test.tsx`**

O Header agora usa `useLocale`, `useTranslations` e renderiza o `LangSwitcher`/`Nav`, que usam hooks de `@/i18n/navigation`. Embrulhar com `renderWithIntl` e mockar a navegação. Conteúdo final:

```tsx
import { screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import { renderWithIntl } from "@/test/intl";
import { Header } from "./Header";

const mockUseHeaderScroll = vi.fn();

vi.mock("@/hooks/useHeaderScroll", () => ({
  useHeaderScroll: () => mockUseHeaderScroll(),
}));

vi.mock("@/i18n/navigation", () => ({
  Link: ({ children, href, ...props }: { children: React.ReactNode; href: string }) => (
    <a href={href} {...props}>{children}</a>
  ),
  usePathname: () => "/",
  useRouter: () => ({ replace: vi.fn() }),
}));

describe("Header", () => {
  it("mantem a navegacao branca enquanto esta sobre hero escuro", () => {
    mockUseHeaderScroll.mockReturnValue({ scrolled: false, onDark: true });
    renderWithIntl(<Header />);
    expect(screen.getByRole("link", { name: "Institucional" })).toHaveClass("text-white");
  });

  it("usa navegacao escura no topo de paginas claras (sem hero escuro)", () => {
    mockUseHeaderScroll.mockReturnValue({ scrolled: false, onDark: false });
    renderWithIntl(<Header />);
    expect(screen.getByRole("link", { name: "Institucional" })).toHaveClass("text-ink-2");
  });

  it("usa navegacao escura quando o header ganha fundo claro no scroll", () => {
    mockUseHeaderScroll.mockReturnValue({ scrolled: true, onDark: false });
    renderWithIntl(<Header />);
    expect(screen.getByRole("link", { name: "Institucional" })).toHaveClass("text-ink-2");
  });
});
```

> O `Nav.test` (Task 8) também precisa do mesmo mock de `@/i18n/navigation` se o `Link` de lá quebrar no jsdom; adicionar o mesmo bloco `vi.mock("@/i18n/navigation", ...)` ao `Nav.test.tsx` caso o Step 6 da Task 8 falhe por falta de contexto de router.

- [ ] **Step 3: Rodar a suíte completa**

Run:
```bash
npx vitest run
```
Expected: todos os testes passam (incluindo Header, Nav, LangSwitcher; os pré-existentes cn/useCarousel/ContatoForm/CaseGrid/etc. seguem verdes). Se `ContatoForm.test` ou outro renderizar um componente que passou a usar `useTranslations`, embrulhar com `renderWithIntl`.

- [ ] **Step 4: Commit**

```bash
git add components/layout/Footer/Footer.tsx components/layout/Header/Header.test.tsx
git commit -m "feat(i18n): Footer via traducoes e testes do header com provider intl"
```

---

## Task 11: Sitemap com alternates de idioma

**Files:**
- Modify: `app/sitemap.ts`

- [ ] **Step 1: Atualizar `app/sitemap.ts` com `alternates.languages`**

```ts
import type { MetadataRoute } from "next";
import { projetos, projetoHref } from "@/app/_sections/Projetos/projetos.data";

const routes = ["", "/institucional", "/atuacao", "/projetos", "/parceiros", "/investidores", "/insights", "/contato"];
const base = "https://opengroup.com.br";

function withAlternates(path: string) {
  return {
    pt: `${base}${path}`,
    en: `${base}/en${path}`,
    es: `${base}/es${path}`,
  };
}

export default function sitemap(): MetadataRoute.Sitemap {
  const staticEntries: MetadataRoute.Sitemap = routes.map((r) => ({
    url: `${base}${r}`,
    changeFrequency: "monthly" as const,
    priority: r === "" ? 1 : 0.7,
    alternates: { languages: withAlternates(r) },
  }));
  const projetoEntries: MetadataRoute.Sitemap = projetos.map((p) => ({
    url: `${base}${projetoHref(p.slug)}`,
    changeFrequency: "monthly" as const,
    priority: 0.8,
    alternates: { languages: withAlternates(projetoHref(p.slug)) },
  }));
  return [...staticEntries, ...projetoEntries];
}
```

- [ ] **Step 2: Verificar tipos**

Run:
```bash
npx tsc --noEmit
```
Expected: sem erros.

- [ ] **Step 3: Commit**

```bash
git add app/sitemap.ts
git commit -m "feat(i18n): sitemap com alternates pt/en/es por rota"
```

---

## Task 12: Gate final de verificação

**Files:** nenhum (verificação).

- [ ] **Step 1: Lint**

Run:
```bash
npx next lint
```
Expected: sem erros.

- [ ] **Step 2: Tipos**

Run:
```bash
npx tsc --noEmit
```
Expected: sem erros.

- [ ] **Step 3: Testes**

Run:
```bash
npx vitest run
```
Expected: toda a suíte verde.

- [ ] **Step 4: Build estático**

Run:
```bash
npm run build
```
Expected: build conclui; rotas geradas para `pt`/`en`/`es`; páginas marcadas como estáticas (○/●), sem forçar dinâmico.

- [ ] **Step 5: Smoke manual no dev server**

Run:
```bash
npm run dev
```
Verificar no browser:
- `/` → conteúdo PT, header com PT ativo.
- `/en` → nav/footer/CTAs em inglês (corpo das páginas ainda PT — esperado nesta fase).
- `/es` → nav/footer/CTAs em espanhol.
- Clicar EN no seletor em `/institucional` leva a `/en/institucional`.

Expected: comportamento acima confirmado. Encerrar o dev server.

> Não commitar nada neste gate; é só verificação. O `npm run build` é parte do gate de fase (não rodar em tweaks pequenos depois).

---

## Notas de handoff para o Plano 2

- Namespaces de página (`home.*`, `institucional.*`, `projetos.*`, `empreendimento.*`, `clientes.*`, `investidores.*`, `insights.*`, `contato.*`, `parceiros.*`, `privacidade.*`) ainda **não existem** — criar ao extrair cada página.
- `metadata` por página continua PT e estático — localizar via `generateMetadata` + `getTranslations` no Plano 2.
- `footerAtuacao` e os textos dos `*.data.ts` de seção seguem em PT.
- Cópia provisória dos `[slug]` permanece PT (decisão do spec).
- O glossário de marca (spec, seção 4) deve ser fechado com o usuário **antes** de iniciar a tradução das páginas.
